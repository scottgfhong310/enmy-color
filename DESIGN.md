# DESIGN — enmy-color

> 版本 v1.0｜最後更新 2026-08-01

README 管「怎麼用」，本文管「**為什麼長這樣**」。家族共同規範只標引用、不重複。

---

## 1. 這支 app 在回答什麼

與另外四支色彩 registry 同一個問題——**「這個色號是什麼？我怎麼把它貼進 CSS？」**——
所以明細卡照 [DESIGN_GUIDELINES §11.1](https://github.com/scottgfhong310/nodeapp-webapp-family/blob/main/DESIGN_GUIDELINES.md)
的骨架逐條對齊，只有 `.d-section` 的段落內容不同。

但 ENMY 有一個前四支都沒有的處境：**它不發佈色名**。這件事決定了本 app 一半的設計。

---

## 2. 沒有色名，所以主識別只有色號

§6.2 立下「官方英文名恆為主名，譯名只作輔助行」，理由是主名必須是**跟賣家溝通、
可驗證的識別憑據**。ENMY 的官方識別只有色號，所以：

- 明細卡的 `.d-name` 多數時候不是空的，而是**明寫**「原廠不發佈色名，此色以色號識別」
  （§11.1：「沒有這項資料要寫出來，不要留白」——留白會被讀成抽取漏了）。
- 4 個膚色在隨盒色卡上有中文標示（`E1 淺膚2`／`E2 膚3`／`E3 膚4`／`DE2 深膚2`），
  顯示在同一格但**不升格為主名**。它們自帶一套與色碼不同的編號（`E1` → 淺膚2），
  照原文記錄、不對齊色碼。
- 一維色票卡與最接近色清單的「名字」那一行，改放官方色系名（或膚色的中文標示），
  **不留空**——空白會被讀成資料掉了，而事實是原廠不發佈。
- `EnmyColorLib.filter` **沒有** `name`／`nameJa` 兩格。不是漏寫：資料裡就沒有那兩個欄位，
  比對不存在的欄位只會讓下一個讀的人以為資料缺了。

**DE1 沒有中文標示，是因為看不清楚不是因為沒有。** 隨盒色卡的照片解析度不足以確讀
（疑為「膚影1」），DB 留 NULL 並在 `fd_note` 說明。寧可缺，不要錯。

---

## 3. 兩個軸：官方色系 vs 色碼字首

ENMY 的色號是**字首＋號碼**（`R1` = `R` + `1`），共 14 個字首。
很自然會想把字首當成「色系」——**但品牌從未公布字首的名稱**。

家族已經有一條相關的規矩：`tb_color_family.fd_name_en` 是 `NOT NULL`，
而「`fd_name_en` 是資料、照抄」。填一個我們自己編的「R = Red」就是造字。

所以兩個軸分開放：

| | 是什麼 | 有沒有官方名稱 | 在 UI 的位置 |
|---|---|---|---|
| **官方色系**（8 個） | 官方店 variant 選項的分群 | ✅ `Red & Pink`／`Blue & Teal`… | **chips** |
| **色碼字首**（14 個） | 色碼自身的結構 | ❌ 品牌未公布 | **列首，只印字首本身** |

版面因此是「一列一個字首、橫向依號碼」——那是把 ENMY 原本的編碼畫出來，
同 `copic-color` 用矩陣畫 Copic Color System，**不是我們發明的版面**。

> 領域庫那一側的同一個決議見 `ARTCOLOR_DOMAIN_GOVERNANCE.md` §9-Q10。
> 教訓可攜：Finecolour 證偽了「每個品牌都發佈**色名**」，代價是改 schema；
> ENMY 證偽「每個品牌都為自己的**色系**命名」，但換一個軸就解決了。
> **先找有官方值的軸，找不到才動 schema。**

---

## 4. 為什麼沒有 `sets.html`

`faber-castell-color`／`copic-color`／`finecolour-color` 都有第二頁「套組收錄對照」，
它回答的是「選一個套組 → 只留下它的色 → 橫向看其他套組還缺幾色」。

ENMY 只有**兩組收錄已知**（60 與 80），而 80 就是全色。那張表的每一格都算得出來，
但答案只有一句話：「60 組相對 80 組少 20 色」。為了一句話開一頁不划算，
所以那句話直接寫進明細卡與色塊上的 `60` 標記。

**24／36／48 三組的收錄清單找到之後，這個判斷就該重新評估**——屆時 5 欄的矩陣
才真的有東西可看。（觸發條件寫在這裡，是為了下一個 session 不必重推一次同樣的推理；
同 `faber-castell-color/DESIGN.md` §1.1 那三條觸發條件的用法，而那條真的被引爆過。）

---

## 5. 「未知」不是「不收錄」

資料檔的 `known:false` 是本 app 最重要的一個欄位。三處都必須分開處理：

1. **明細卡**：只畫收錄已知的兩組徽章，另起一行說「另有 24／36／48 三組，
   但沒有任何來源寫出它們收錄哪些色——不是『不收錄』，是未知」。
2. **色塊**：只標 `60`（已知的那一組）。若比照畫成「不在 24 組」就是造假。
3. **最接近色比對**：收錄不明的套組**不列進下拉選單**；`nearestENMY` 遇到
   `known:false` 一律**忽略該過濾條件**而不是回空陣列——
   **空結果會被讀成「沒有接近的色」，那是錯的答案，不是空的答案。**

---

## 6. 逐色的來源等級

80 色裡有 60 色同時出現在兩份獨立的官方色表，且兩份 hex **逐位元相同**（ΔE00 = 0.00）；
另外 20 色只有一份來源。這個差別是使用者唯一能據以判斷「這個 hex 該信幾分」的線索，
所以**逐色標示**（`verify` 欄），明細卡的精度聲明也因此不是一句寫死的話。

⚠️ 兩種都不是規格：色表是**行銷素材**，品牌未聲明它等於墨水實色。
比 Finecolour 的印刷色譜好（平色數位填色、非印刷取樣），但比 Faber-Castell
從官方色卡 PDF 讀向量填色差一級——那是色彩參考文件，這是商品頁圖。

---

## 7. 沒有的欄位是刻意留著的一段

明細卡有一段「這個品牌沒有發佈的」，逐條寫出耐光度／顏料／英日文色名都不存在。

這段存在的理由與 §11.1 的 `.d-empty` 同源：**家族另外四支有的欄位，這裡不是漏抽**。
不寫出來，讀者會以為是資料沒補齊；寫出來，它就變成一項關於這個品牌的事實。
